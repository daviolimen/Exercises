#include <bits/stdc++.h>
using namespace std;

#define MAXN 100001

int N, M, S[MAXN];



int main() {
    cin >> N >> M;
    for (int i = 1; i <= N; i++) cin >> S[i];

    while (M--) {
        int T; cin >> T;
        if (T == 1) {
            int I, V; cin >> I >> V;

        } else if (T == 2) {
            int E, D; cin >> E >> D;

        }
    }
}