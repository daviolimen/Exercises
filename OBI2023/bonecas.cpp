#include <bits/stdc++.h>
using namespace std;

const int MAXN = 10001, MAXK = 3001;

int n, k, tam[MAXN], ctr[100001];

int main() {
    cin >> n >> k;
    for (int i = 0; i < n; i++) {
        cin >> tam[i];
        ctr[tam[i]]++;
    }

    
}