#include <bits/stdc++.h>
using namespace std;

typedef pair<int, int> pi;

#define MAXN 100001

int N, M, A, B;
vector<pi> graph[MAXN];
bool visited[MAXN];

int main() {
    cin >> N >> M >> A >> B;
    for (int i = 0; i < M; i++) {
        
    }
}