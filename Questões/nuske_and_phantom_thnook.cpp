#include <bits/stdc++.h>
using namespace std;

#define MAXN 2001

int n, m, q, prefix[MAXN][MAXN];
char grid[MAXN][MAXN];

int main() {
    cin >> n >> m >> q;
    for (int i = 1; i <= n; i++) {
        for (int j = 1; j <= m; j++) {
            cin >> grid[i][j];
        }
    }

    for (int i = 1; i <= n; i++) {
        for (int j = 1; j <= m; j++) {
            
        }
    }
}