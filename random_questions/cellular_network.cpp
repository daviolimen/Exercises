#include <bits/stdc++.h>
using namespace std;

#define MAXN 100000

int n, m, a[MAXN], b[MAXN];

int main() {
    cin >> n >> m;
    for (int i = 0; i < n; i++) {
        cin >> a[i];
    }
    for (int i = 0; i < m; i++) {
        cin >> b[i];
    }

    for (int i = 0; i < n; i++) {
        
    }
}